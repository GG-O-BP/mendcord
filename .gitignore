*.beam
*.ez
/build
erl_crash.dump

# Secrets
.env
.env.*
!.env.example

# Runtime state (persisted via Actions cache in CI)
seen.json

# Claude Code: keep shared config tracked, ignore per-dev local overrides
.claude/settings.local.json
