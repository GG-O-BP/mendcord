---
description: Run gleam format on the whole project.
argument-hint: (no arguments)
allowed-tools: Bash(gleam format:*)
---

Run `gleam format` on the project root and report which files were changed (from `git diff --stat` if available). One-line summary if no files changed.
